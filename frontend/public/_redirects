# SPA fallback for Cloudflare Pages: serve index.html for client-side routes.
# /api/* and /uploads/* are handled by Pages Functions (see functions/) before
# this rule applies, so they are not shadowed.
/*  /index.html  200
